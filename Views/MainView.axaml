<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Library.ViewModels"
             xmlns:views="using:Library.Views"
             x:Class="Library.Views.MainView"
             x:DataType="vm:MainWindowViewModel"
             Background="{DynamicResource ThemeBackgroundBrush}">

  <DockPanel>
    <Border DockPanel.Dock="Top"
            Background="{DynamicResource ThemeSurfaceHighBrush}"
            BorderBrush="{DynamicResource ThemeBorderBrush}"
            BorderThickness="0,0,0,1"
            Padding="14,0"
            MinHeight="40"
            IsVisible="{Binding !IsCommanderMode}">
      <DockPanel LastChildFill="False">
        <TextBlock DockPanel.Dock="Left"
                   Text="MTG Library"
                   FontWeight="SemiBold"
                   FontSize="13"
                   Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                   VerticalAlignment="Center" />
        <StackPanel DockPanel.Dock="Right"
                    Orientation="Horizontal"
                    Spacing="8"
                    VerticalAlignment="Center">
          <TextBlock Text="Commander Players"
                     FontSize="11"
                     Opacity="0.6"
                     Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                     VerticalAlignment="Center" />
          <ComboBox ItemsSource="{Binding CommanderLife.PlayerCountOptions}"
                    SelectedItem="{Binding CommanderLife.SelectedPlayerCount}"
                    Width="70"
                    Height="28"
                    FontSize="12"
                    VerticalContentAlignment="Center" />
          <Button Content="Commander Mode"
                  Command="{Binding EnterCommanderModeCommand}"
                  Padding="10,4"
                  Height="28" />
          <Button Content="Import CSV"
                  Command="{Binding ImportCsvCommand}"
                  Padding="10,4"
                  Height="28"
                  IsVisible="{OnPlatform iOS=True, Android=True, Default=False}" />
          <TextBlock Text="{Binding ImportStatus}"
                     IsVisible="{Binding HasImportStatus}"
                     FontSize="11"
                     VerticalAlignment="Center"
                     Margin="4,0,0,0"
                     Foreground="{DynamicResource ThemeTextSecondaryBrush}" />
          <TextBlock Text="Theme"
                     FontSize="11"
                     Opacity="0.6"
                     Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                     VerticalAlignment="Center" />
          <Border Background="#2A3E6A"
                  BorderBrush="#5F9CFF"
                  BorderThickness="1"
                  CornerRadius="10"
                  Padding="8,2"
                  IsVisible="{Binding ShowIosThemeRecommendation}">
            <TextBlock Text="{Binding IosThemeRecommendationText}"
                       FontSize="10"
                       FontWeight="SemiBold"
                       Foreground="#DCEBFF"
                       VerticalAlignment="Center" />
          </Border>
          <ComboBox ItemsSource="{Binding AvailableThemes}"
                    SelectedItem="{Binding CurrentTheme}"
                    Width="130"
                    Height="28"
                    FontSize="12"
                    VerticalContentAlignment="Center" />
        </StackPanel>
      </DockPanel>
    </Border>

    <TabControl SelectedIndex="{Binding SelectedTabIndex}"
                IsVisible="{Binding !IsCommanderMode}">
      <TabItem Header="Collection">
        <views:CollectionView DataContext="{Binding Collection}" />
      </TabItem>
      <TabItem Header="Decks">
        <views:DecksView DataContext="{Binding Decks}" />
      </TabItem>
      <TabItem Header="Binders">
        <views:BindersView DataContext="{Binding Binders}" />
      </TabItem>
      <TabItem Header="Shopping">
        <views:ShoppingView DataContext="{Binding Shopping}" />
      </TabItem>
    </TabControl>

    <Grid IsVisible="{Binding IsCommanderMode}">
      <views:CommanderLifeView DataContext="{Binding CommanderLife}" />
      <Button Content="← Back"
              Command="{Binding ExitCommanderModeCommand}"
              HorizontalAlignment="Left"
              VerticalAlignment="Top"
              Margin="12"
              Padding="12,6"
              FontSize="14"
              FontWeight="SemiBold"
              Background="#AA000000"
              Foreground="White" />
    </Grid>
  </DockPanel>
</UserControl>
